#glpidata:
#  image: ubuntu
#  volumes:
#    - /var/www/html:/var/www/html/glpi


# PHP-Apache container
glpi:
#  image: php
  build: ./php
#  stdin_open: true
#  tty: true
  ports:
    - "8090:80"
  links:
    - mysql:db
#  volumes_from:
#    - glpidata
  volumes:
    - /opt/docker/glpi/glpi:/var/www/html


# Database persistent data
mysqldata:
  image: ubuntu
  volumes:
    - /var/lib/mysql

# Database container
mysql:
  image: mariadb
#  stdin_open: true
#  tty: true
#  ports:
#    - "3307:3306"
  environment:
    - MYSQL_ROOT_PASSWORD=arcom1973
    - MYSQL_DATABASE=glpiatualizacao
  volumes_from:
    - mysqldata
  volumes:
    - /opt/docker/glpi/database:/docker-entrypoint-initdb.d
